# Coach API (Cloudflare Worker → free Groq/Llama)
# Never put GROQ_API_KEY here — browser-visible. Key stays on the worker only.
# Deploy: see coach-api/README.md
# Example: https://astronavigator-coach.<account>.workers.dev
VITE_COACH_API_URL=
