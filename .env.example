# Optional Google Analytics 4 measurement ID (loads only after cookie consent)
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Never put LLM API keys in the frontend. SpaceXAI / xAI keys belong on a server only.
# XAI_API_KEY=
